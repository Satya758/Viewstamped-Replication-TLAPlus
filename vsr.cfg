
\* Though we have 5 servers we only consider 3 for now
CONSTANTS 
    Servers = {1, 2, 3}

    Nil = 0

    \* Server Status
    PrimaryNormal = PrimaryNormal
    ReplicaNormal = ReplicaNormal
    ViewChange = ViewChange
    Recovering = Recovering

    \* Message types
    RequestMsg = RequestMsg
    PrepareMsg = PrepareMsg
    PrepareOkMsg = PrepareOkMsg
    ReplyMsg = ReplyMsg
    CommitMsg = CommitMsg
    StartViewChangeMsg = StartViewChangeMsg
    DoViewChangeMsg = DoViewChangeMsg
    StartViewMsg = StartViewMsg
    RecoveryMsg = RecoveryMsg
    RecoveryResponseMsg = RecoveryResponseMsg
    GetStateMsg = GetStateMsg
    NewStateMsg = NewStateMsg
    ReconfigurationMsg = ReconfigurationMsg
    StartEpochMsg = StartEpochMsg
    EpochStartedMsg = EpochStartedMsg
    CheckEpochMsg = CheckEpochMsg
    NewEpochMsg = NewEpochMsg

INIT Init
NEXT Next



CONSTRAINTS 

INVARIANTS 